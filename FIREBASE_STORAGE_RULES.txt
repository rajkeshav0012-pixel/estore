rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow read access to all files (for public images)
    match /{allPaths=**} {
      allow read: if true;
    }
    
    // Allow write access to products folder (for development)
    match /products/{fileName} {
      allow write: if true;
    }
    
    // You can add more specific rules later for production:
    // match /products/{fileName} {
    //   allow write: if request.auth != null 
    //     && request.resource.size < 5 * 1024 * 1024 // 5MB limit
    //     && request.resource.contentType.matches('image/.*');
    // }
  }
}